<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}memoire online {% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {%   include 'link/link1_css.html.twig'  %}
    </head>
    
        {% block body %}
        <div class="container">
          <div class="row" style="margin-top:5%">
           <div class="col-xl-6 col-lg-6 col-md-6 col-sm-6" style="padding-left:20%">
               <div class="card text-white bg-success mb-3">
                    <div class="card-content">
                        <div class="card-body">
                                    <h2 class="card-title" style="text-align:center ; font-size:24px ; color:white">JE CREE MON COMPTE </h2>
                                    <p class="card-text">
                                        
                                    </p>
                                    {{ form_start(registrationForm) }}
                                    {{ form_end(registrationForm) }}   
                                <div class="text-center mt-5 text-lg fs-4">
                                    <p class="text-compte ">Avez-vous dejà un compte ? <br> 
                                    <a class="btn btn-lg btn-primary"  href="{{ path('app_login') }}" style="color:white"> Se connecter </a>
                               </div>    
                        </div>
                    </div>
                </div>
           </div>
          </div>
        </div>
     {% endblock %}
 {%   include 'link/link_js.html.twig'   %} 
    </body>
</html>

